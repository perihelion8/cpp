#ifndef PHONEBOOK_HPP
#define PHONEBOOK_HPP

#include "Contact.hpp"

class PhoneBook {
 public:
  PhoneBook();
  void AddContact();
  void SearchContacts();

 private:

  void DisplayContacts();
  void DisplayContact(int index);

  int index_;
  bool is_full_;
  int contacts_size_;
  int table_fields_;
  Contact contacts_[contacts_size_];
};

#endif
